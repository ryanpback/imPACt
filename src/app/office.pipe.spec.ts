/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OfficePipe } from './office.pipe';

describe('OfficePipe', () => {
  it('create an instance', () => {
    const pipe = new OfficePipe();
    expect(pipe).toBeTruthy();
  });
});
