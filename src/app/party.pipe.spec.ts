/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PartyPipe } from './party.pipe';

describe('PartyPipe', () => {
  it('create an instance', () => {
    const pipe = new PartyPipe();
    expect(pipe).toBeTruthy();
  });
});
